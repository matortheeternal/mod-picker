<div class="scaffold">
  <p id="notice"><%= notice %></p>

  <h1>Listing mod_list_comments</h1>

  <table>
    <thead>
      <tr>
          <th>#<Rails::Generators::GeneratedAttribute:0x00000007bed7f8></th>
          <th>#<Rails::Generators::GeneratedAttribute:0x00000007bed578></th>
          <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @mod_list_comments.each do |mod_list_comment| %>
        <tr>
            <td><%= mod_list_comment.ml_id %></td>
            <td><%= mod_list_comment.c_id %></td>
            <td><%= link_to 'Show', mod_list_comment %></td>
          <td><%= link_to 'Edit', edit_mod_list_comment_path(mod_list_comment) %></td>
          <td><%= link_to 'Destroy', mod_list_comment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to 'New mod_list_comment', new_mod_list_comment_path %>
</div>