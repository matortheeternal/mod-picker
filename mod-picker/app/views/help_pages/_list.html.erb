<ul class="pages-list">
    <% @help_pages.each do |page| %>
    <li>
        <% if page.display_image %>
        <%= link_to "/help/#{page.url_name}", method: :get do %>
            <%= image_tag "#{@help_page.display_image}", class: "header-image" %>
        <% end %>
        <% end %>

        <div class="meta-container">
            <%= link_to "/help/#{page.url_name}", method: :get do %>
                <h4 class="item-title"><%= page.name.capitalize %></h4>
            <% end %>


            <span class="meta author" title="Author">
                <i class="fa fa-user" aria-hidden="true"></i>
                <%= link_to page.submitter.username, user_path(page.submitter) %>
            </span>

            <span class="meta comment">
                <% if page.comments_count > 0 %>
                <i class="fa fa-comment" aria-hidden="true"></i>
                <% else %>
                <i class="fa fa-comment-o" aria-hidden="true"></i>
                <% end %>
                <%= page.comments_count %>
            </span>

            <div class="date">
                <span class="meta submit-date" title="<%= page.submitted.strftime("%m/%d/%Y %I:%M:%S %z") %>">
                    <i class="fa fa-clock-o" aria-hidden="true"></i>
                    <%= page.submitted.strftime("Created %b %e %Y") %>
                    <% if page.submitted.between?((DateTime.now - 2.week), DateTime.now) %>
                    <span class="new-label">NEW</span>
                    <% end %>
                </span>
            </div>

            <% if page.game.present? %>
            <span class="meta game" title="Game">
                <i class="fa fa-gamepad" aria-hidden="true"></i>
                <%= link_to page.game.display_name, "/help/game/#{page.game.display_name}" %>
            </span>
            <% end %>

            <span class="meta category" title="Category">
                <i class="fa fa-folder" aria-hidden="true"></i>
                <%= link_to page.category.humanize, "/help/category/#{page.category}" %>
            </span>
        </div>
    </li>
    <% end %>
</ul>
