<%= form_for(@mod_comment) do |f| %>
  <% if @mod_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mod_comment.errors.count, "error") %> prohibited this mod_comment from being saved:</h2>

      <ul>
      <% @mod_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :mod_id %><br>
    <%= f.number_field :mod_id %>
  </div>
  <div class="field">
    <%= f.label :comment_id %><br>
    <%= f.number_field :comment_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>