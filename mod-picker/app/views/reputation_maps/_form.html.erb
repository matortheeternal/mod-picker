<%= form_for(@reputation_map) do |f| %>
  <% if @reputation_map.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reputation_map.errors.count, "error") %> prohibited this reputation_map from being saved:</h2>

      <ul>
      <% @reputation_map.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :from_rep_id %><br>
    <%= f.number_field :from_rep_id %>
  </div>
  <div class="field">
    <%= f.label :to_rep_id %><br>
    <%= f.number_field :to_rep_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
